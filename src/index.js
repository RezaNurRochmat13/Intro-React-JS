import React from 'react';
import ReactDOM from 'react-dom';

/* function formatName(user){
    return user.firstName + ' '+ user.lastName;
}

const user = {
    firstName: 'Harper',
    lastName: 'Rubi'
};
 */
const element = (
    // <h1>Hello, {formatName(user)}!</h1>
    <h2>Rizky ganteng sangat</h2>
  );
  

ReactDOM.render(
    element,
    document.getElementById('root')
);